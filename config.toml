baseurl = "https://ihacktoprotect.com/" # Controls base URL
languageCode = "en-US" # Controls site language
title = "I hack to protect" # Homepage title and page title suffix
paginate = 11 # Number of posts to show before paginating
theme = "after-dark" # Set default theme
enableRobotsTXT = true # Suggested, enable robots.txt file
googleAnalytics = "" # Optional, add tracking Id for analytics
disqusShortname = "" # Optional, add Disqus shortname for comments
sectionPagesMenu = "false" # Enable menu system for lazy bloggers - put "main" as a value to enable back again the pre defined menus
footnoteReturnLinkContents = "↩" # Provides a nicer footnote return link

[params]
  description = "" # Suggested, controls default description meta
  author = "MarioAreias" # Optional, controls author name display on posts
  hide_author = false # Optional, set true to hide author name on posts
  show_menu = true # Optional, set true to enable section menu
  powered_by = false # Optional, set false to disable credits
  images = [
    "https://source.unsplash.com/collection/983219/2000x1322"
  ] # Suggested, controls default Open Graph images

[params.hackcss]
  disabled = false # Optional, set `true` to disable hackcss
  mode = "hack" # Optional, choose from `standard` or `hack` display modes
  palette = "dark-grey" # Optional, choose `dark`, `dark-grey` or `solarized-dark` 

[menu]

[[menu.main]]
  name = "Home"
  weight = 1
  identifier = "home"
  url = "/"
[[menu.main]]
  name = "About"
  weight = 2
  identifier = "about"
  url = "/page/about"
[[menu.main]]
  name = "Categories"
  weight = 3
  identifier = "categories"
  url = "/categories"
[[menu.main]]
  name = "Tags"
  weight = 4
  identifier = "tags"
  url = "/tags"

[blackfriday]
  hrefTargetBlank = true
